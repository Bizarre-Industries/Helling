# Helling project docs (local only)
helling.md
helling-complete.md
IMPLEMENTATION-PLAN.md

# Editor and agent caches (project-local; never tracked)
# .claude/settings.json is project-shared (PostToolUse version-ship hook per ADR-053).
# Everything else under .claude/ is per-contributor / per-machine local state.
.claude/agents/
.claude/launch.json
.claude/rules/
.claude/settings.local.json
.claude/worktrees/
.claude/.last-shipped-tag

# autoskills install dir — source of truth lives upstream; we deploy real
# copies into .claude/skills/ which Claude Code reads. Re-sync via
# `cp -RL .agents/skills/. .claude/skills/` after running the autoskills tool.
.agents/
.claude-cache/
.claude-plans/
.design-fetch/
.remember/
.task/
LESSONS.md

# Go
/bin/
/dist/
*.exe
*.dll
*.so
*.dylib
*.test
*.out
vendor/
go.work.sum

# Generated API code is committed; drift checks fail when these outputs change.

# Stray binaries at root
/hellingd
/helling-cli

# SvelteKit / Node
web/node_modules/
web/build/
web/dist/
web/package-lock.json
web/*.tsbuildinfo
web/.dev.log

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Test artifacts
coverage.out
*.prof

# Local dev SQLite artifacts
.tmp/
*.db
*.db-wal
*.db-shm
helling-dev.db*

# Secrets
*.secret
*.key
*.pem
jwt.secret

# Local environment files
.env
.env.local
.env.*.local

# Misc
refs/


api/openapi.yaml.tmp

# Phase 0 parity exceptions.
#
# Operations listed here are NOT yet implemented in CLI or WebUI.
# Each entry must declare which surface is missing and a target version.
# Remove the entry once the surface is implemented and tested.
#
# Fields:
#   operation_id  — operationId from api/openapi.yaml
#   missing       — "cli", "webui", or "both"
#   reason        — why the exception exists
#   target_version — first release that should close the gap

exceptions:
  # ── Auth ───────────────────────────────────────────────────────────────────
  # authLogin / authLogout: server-side implemented in PR B
  # (argon2id + Ed25519 JWT + cookie-backed sessions per docs/spec/auth.md §2).
  # CLI surface lands in PR F, WebUI in PR G.
  - operation_id: authLogin
    missing: webui
    reason: "Server-side PR B; CLI PR F (`helling auth login`); WebUI pending"
    target_version: "v0.1.0-alpha"

  - operation_id: authLogout
    missing: webui
    reason: "Server-side PR B; CLI PR F (`helling auth logout`); WebUI pending"
    target_version: "v0.1.0-alpha"

  # authMfaComplete / authTotp{Setup,Verify,Disable}: server-side in PR C.
  # CLI shipped in PR W (`helling auth mfa setup|verify|disable`); login MFA
  # prompt in PR F. WebUI still pending.
  - operation_id: authMfaComplete
    missing: webui
    reason: "Server-side PR C; CLI MFA prompt PR F + `helling auth mfa` PR W; WebUI pending"
    target_version: "v0.1.0-alpha"

  - operation_id: authTotpSetup
    missing: webui
    reason: "Server-side PR C; CLI PR W (`helling auth mfa setup`); WebUI pending"
    target_version: "v0.1.0-alpha"

  - operation_id: authTotpVerify
    missing: webui
    reason: "Server-side PR C; CLI PR W (`helling auth mfa verify`); WebUI pending"
    target_version: "v0.1.0-alpha"

  - operation_id: authTotpDisable
    missing: webui
    reason: "Server-side PR C; CLI PR W (`helling auth mfa disable`); WebUI pending"
    target_version: "v0.1.0-alpha"

  # authToken{List,Create,Revoke}: CLI shipped in PR V.
  - operation_id: authTokenList
    missing: webui
    reason: "Server-side PR C; CLI PR V (`helling auth token list`); WebUI pending"
    target_version: "v0.1.0-alpha"

  - operation_id: authTokenCreate
    missing: webui
    reason: "Server-side PR C; CLI PR V (`helling auth token create`); WebUI pending"
    target_version: "v0.1.0-alpha"

  - operation_id: authTokenRevoke
    missing: webui
    reason: "Server-side PR C; CLI PR V (`helling auth token revoke`); WebUI pending"
    target_version: "v0.1.0-alpha"

  # ── Users ──────────────────────────────────────────────────────────────────
  - operation_id: userList
    missing: webui
    reason: "Server-side PR I; CLI PR T (`helling user list`); WebUI pending"
    target_version: "v0.1.0-alpha"

  - operation_id: userCreate
    missing: webui
    reason: "Server-side PR I; CLI PR T (`helling user create`); WebUI pending"
    target_version: "v0.1.0-alpha"

  - operation_id: userDelete
    missing: webui
    reason: "Server-side PR I; CLI PR T (`helling user delete`); WebUI pending"
    target_version: "v0.1.0-alpha"

  - operation_id: userSetScope
    missing: webui
    reason: "Server-side PR I; CLI PR T (`helling user set-scope`); WebUI pending"
    target_version: "v0.1.0-alpha"

  # ── Schedules ──────────────────────────────────────────────────────────────
  - operation_id: scheduleList
    missing: both
    reason: "Phase 0 — schedule surfaces not yet implemented"
    target_version: "v0.5.0"

  - operation_id: scheduleCreate
    missing: both
    reason: "Phase 0 — schedule surfaces not yet implemented"
    target_version: "v0.5.0"

  - operation_id: scheduleGet
    missing: both
    reason: "Phase 0 — schedule surfaces not yet implemented"
    target_version: "v0.5.0"

  - operation_id: scheduleUpdate
    missing: both
    reason: "Phase 0 — schedule surfaces not yet implemented"
    target_version: "v0.5.0"

  - operation_id: scheduleDelete
    missing: both
    reason: "Phase 0 — schedule surfaces not yet implemented"
    target_version: "v0.5.0"

  - operation_id: scheduleRunNow
    missing: both
    reason: "Phase 0 — schedule surfaces not yet implemented"
    target_version: "v0.5.0"

  # ── Webhooks ───────────────────────────────────────────────────────────────
  - operation_id: webhookList
    missing: webui
    reason: "Server-side PR K; CLI PR T (`helling webhook ...`); WebUI pending"
    target_version: "v0.1.0-alpha"

  - operation_id: webhookCreate
    missing: webui
    reason: "Server-side PR K; CLI PR T (`helling webhook ...`); WebUI pending"
    target_version: "v0.1.0-alpha"

  - operation_id: webhookGet
    missing: webui
    reason: "Server-side PR K; CLI PR T (`helling webhook ...`); WebUI pending"
    target_version: "v0.1.0-alpha"

  - operation_id: webhookUpdate
    missing: webui
    reason: "Server-side PR K; CLI PR T (`helling webhook ...`); WebUI pending"
    target_version: "v0.1.0-alpha"

  - operation_id: webhookDelete
    missing: webui
    reason: "Server-side PR K; CLI PR T (`helling webhook ...`); WebUI pending"
    target_version: "v0.1.0-alpha"

  - operation_id: webhookTest
    missing: webui
    reason: "Server-side PR K; CLI PR T (`helling webhook ...`); WebUI pending"
    target_version: "v0.1.0-alpha"

  # ── Kubernetes ─────────────────────────────────────────────────────────────
  - operation_id: kubernetesList
    missing: both
    reason: "Phase 0 — Kubernetes surfaces not yet implemented"
    target_version: "v0.6.0"

  - operation_id: kubernetesCreate
    missing: both
    reason: "Phase 0 — Kubernetes surfaces not yet implemented"
    target_version: "v0.6.0"

  - operation_id: kubernetesGet
    missing: both
    reason: "Phase 0 — Kubernetes surfaces not yet implemented"
    target_version: "v0.6.0"

  - operation_id: kubernetesDelete
    missing: both
    reason: "Phase 0 — Kubernetes surfaces not yet implemented"
    target_version: "v0.6.0"

  - operation_id: kubernetesScale
    missing: both
    reason: "Phase 0 — Kubernetes surfaces not yet implemented"
    target_version: "v0.6.0"

  - operation_id: kubernetesUpgrade
    missing: both
    reason: "Phase 0 — Kubernetes surfaces not yet implemented"
    target_version: "v0.6.0"

  - operation_id: kubernetesKubeconfig
    missing: both
    reason: "Phase 0 — Kubernetes surfaces not yet implemented"
    target_version: "v0.6.0"

  # ── System ─────────────────────────────────────────────────────────────────
  - operation_id: systemInfo
    missing: webui
    reason: "Server-side PR J; CLI PR T/U (`helling system ...` + `helling system health`); WebUI pending"
    target_version: "v0.1.0-alpha"

  - operation_id: systemHardware
    missing: webui
    reason: "Server-side PR J; CLI PR T/U (`helling system ...` + `helling system health`); WebUI pending"
    target_version: "v0.1.0-alpha"

  - operation_id: systemConfigGet
    missing: webui
    reason: "Server-side PR J; CLI PR T/U (`helling system ...` + `helling system health`); WebUI pending"
    target_version: "v0.1.0-alpha"

  - operation_id: systemConfigPut
    missing: webui
    reason: "Server-side PR J; CLI PR T/U (`helling system ...` + `helling system health`); WebUI pending"
    target_version: "v0.1.0-alpha"

  - operation_id: systemUpgrade
    missing: webui
    reason: "Server-side PR J; CLI PR T/U (`helling system ...` + `helling system health`); WebUI pending"
    target_version: "v0.1.0-alpha"

  - operation_id: systemDiagnostics
    missing: webui
    reason: "Server-side PR J; CLI PR T/U (`helling system ...` + `helling system health`); WebUI pending"
    target_version: "v0.1.0-alpha"

  # ── Firewall ───────────────────────────────────────────────────────────────
  - operation_id: firewallHostList
    missing: both
    reason: "Phase 0 — firewall surfaces not yet implemented"
    target_version: "v0.5.0"

  - operation_id: firewallHostCreate
    missing: both
    reason: "Phase 0 — firewall surfaces not yet implemented"
    target_version: "v0.5.0"

  - operation_id: firewallHostDelete
    missing: both
    reason: "Phase 0 — firewall surfaces not yet implemented"
    target_version: "v0.5.0"

  # ── Audit ──────────────────────────────────────────────────────────────────
  # auditQuery and auditExport have CLI surface (`helling audit query`,
  # `helling audit export`) and WebUI surface (/audit page). Parity gap closed.

  # ── Health / Events ────────────────────────────────────────────────────────
  # healthGet has CLI surface (`helling system health`) and internal WebUI
  # consumption via app-shell health check. Parity gap closed.

  # eventsSse has CLI surface (`helling events tail`) and is consumed
  # internally by the WebUI for task/log streaming. Parity gap closed.

  # ── v0.1 vertical-slice (docs/v0.1.md, openapi.yaml rewrite 2026-05-03) ────
  # CLI commands land in v0.1.0-beta Stage 5; WebUI pages in Stage 4.
  # Server stubs return 501 until real handlers ship per docs/v0.1.md Stage 3.
  - operation_id: getHealth
    missing: both
    reason: "v0.1 spec; CLI helling health and WebUI healthz indicator land in v0.1.0-beta"
    target_version: "v0.1.0-beta"

  - operation_id: getVersion
    missing: both
    reason: "v0.1 spec; CLI helling version exists but spec sync pending; WebUI About panel in v0.1.0-beta"
    target_version: "v0.1.0-beta"

  - operation_id: logout
    missing: webui
    reason: "v0.1 spec; CLI helling auth logout shipped; WebUI logout button in v0.1.0-beta"
    target_version: "v0.1.0-beta"

  - operation_id: getCurrentUser
    missing: both
    reason: "v0.1 spec; CLI auth whoami exists but spec sync pending; WebUI top-bar profile in v0.1.0-beta"
    target_version: "v0.1.0-beta"

  - operation_id: listInstances
    missing: both
    reason: "v0.1 spec; CLI helling instance ls and WebUI Instances page land in v0.1.0-beta Stages 3-5"
    target_version: "v0.1.0-beta"

  - operation_id: createInstance
    missing: cli
    reason: "v0.1 spec; CLI helling instance create lands in v0.1.0-beta Stage 5"
    target_version: "v0.1.0-beta"

  - operation_id: getInstance
    missing: both
    reason: "v0.1 spec; CLI helling instance get and WebUI Instance Detail in v0.1.0-beta"
    target_version: "v0.1.0-beta"

  - operation_id: deleteInstance
    missing: both
    reason: "v0.1 spec; CLI helling instance rm and WebUI Delete action in v0.1.0-beta"
    target_version: "v0.1.0-beta"

  - operation_id: startInstance
    missing: both
    reason: "v0.1 spec; CLI helling instance start and WebUI Start button in v0.1.0-beta"
    target_version: "v0.1.0-beta"

  - operation_id: stopInstance
    missing: both
    reason: "v0.1 spec; CLI helling instance stop and WebUI Stop button in v0.1.0-beta"
    target_version: "v0.1.0-beta"

  - operation_id: listOperations
    missing: both
    reason: "v0.1 spec; CLI helling op ls and WebUI Operations panel in v0.1.0-beta"
    target_version: "v0.1.0-beta"

  - operation_id: getOperation
    missing: both
    reason: "v0.1 spec; CLI helling op get and WebUI poll-on-detail in v0.1.0-beta"
    target_version: "v0.1.0-beta"

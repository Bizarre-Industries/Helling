# Runbook: PAM Misconfiguration (Deferred)

Helling v0.1 does not use PAM. This placeholder remains so historical links do
not break while the runbook set is revised for local Helling-managed users.

Use `docs/spec/auth.md` and `docs/standards/security.md` for current v0.1 auth
operations.

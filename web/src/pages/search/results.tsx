// Helling WebUI — Search results route alias (Phase 2A).
//
// Same as `index.tsx`; kept as a separate route so the URL
// /search/results can deep-link the same content. Replaces the
// PageSearchResults stub at web/src/pages2.jsx.

import PageSearch from './index';

export default PageSearch;
